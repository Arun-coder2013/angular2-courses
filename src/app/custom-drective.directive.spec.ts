import { CustomDrectiveDirective } from './custom-drective.directive';

describe('CustomDrectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDrectiveDirective();
    expect(directive).toBeTruthy();
  });
});
