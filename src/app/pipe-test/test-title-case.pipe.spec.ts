import { TestTitleCasePipe } from './test-title-case.pipe';

describe('TestTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TestTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
